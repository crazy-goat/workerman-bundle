# Findings — review — issue #593

| # | File:line | What is wrong | Severity | Status |
|---|-----------|---------------|----------|--------|
| F-01 | `bin/check-exception-usage.php:155` | Type-discovery regex matches "interface"/"class" inside docblock comments, capturing the wrong word. Both marker interfaces (`ClientInputExceptionInterface`, `WorkermanExceptionInterface`) are silently unchecked — the regex captures `for` from their docblocks ("Marker interface for …"), and `for` matches everywhere as a PHP keyword. The gate passes vacuously for these two types. | high | open |
| F-02 | `bin/check-exception-usage.php:155` | `preg_match` captures only the first declaration per file. If a file declares multiple types, subsequent ones are silently skipped. No current file has multiple declarations, so latent only. | medium | open |
| F-03 | `bin/check-exception-usage.php:155` | Regex does not explicitly handle `readonly class` / `final readonly class` / `abstract readonly class`. Works by accident via the bare `class` alternative. Latent gap. | low | open |
| F-04 | `UPGRADE.md:498-517` | Hierarchy tree under "Upgrading to 0.12" is missing `MalformedRequestException`, `SfxExtractionException`, `UnsupportedListenSchemeException`, `ClientInputExceptionInterface`. Pre-existing, not introduced by this PR. | low | open |
| F-05 | `CHANGELOG.md:1507` | Released `## [0.12.0]` entry still names deleted `ConfigurationValidationException`. Coder chose to keep as immutable historical record; `[Unreleased]` removal entry added. Judgment call — practical impact low. | low | open |
| F-06 | `tests/ExceptionUsageLintTest.php:199-210` | `removeRecursively` does not suppress or check `rmdir()`/`unlink()` return values; could emit warnings on teardown if permissions are restrictive. | nit | open |
| F-07 | `tests/ExceptionUsageLintTest.php:173` | `runScript` passes only `PATH` to the subprocess; fragile if the script later reads an env var. | nit | open |

## Round 2

| F-01 | `bin/check-exception-usage.php:155` | Type-discovery regex matches "interface"/"class" inside docblock comments, capturing the wrong word. | high | **fixed** — regex replaced with `token_get_all()`; `T_INTERFACE`/`T_CLASS` are real parser tokens that cannot appear inside comments or strings. Regression test `testTheWordInterfaceInADocblockIsNotMistakenForADeclaration` added. |
| F-02 | `bin/check-exception-usage.php:155` | `preg_match` captures only the first declaration per file. | medium | **fixed** — `token_get_all` loop iterates all tokens, finding every `T_INTERFACE`/`T_CLASS`. Regression test `testMultipleTypesInOneFileAreAllDiscovered` added. |
| F-03 | `bin/check-exception-usage.php:155` | Regex does not explicitly handle `readonly class` / `final readonly class` / `abstract readonly class`. | low | **fixed** — `token_get_all` handles all modifier combinations naturally (`readonly`, `final`, `abstract` are separate tokens preceding `T_CLASS`). Verified with `php -r`. |
| F-04 | `UPGRADE.md:498-517` | Hierarchy tree under "Upgrading to 0.12" missing `MalformedRequestException`, `SfxExtractionException`, `UnsupportedListenSchemeException`, `ClientInputExceptionInterface`. Pre-existing. | low | **fixed** — all 4 types added to the tree. Before/After table also corrected: `MalformedRequestException` added to `\InvalidArgumentException` row, `SfxExtractionException` + `UnsupportedListenSchemeException` added to `\RuntimeException` row. All 22 types verified present in the tree. |
| F-05 | `CHANGELOG.md:1507` | Released `## [0.12.0]` entry still names deleted `ConfigurationValidationException`. | low | **still present** — coder's explicit judgment call: released changelogs are immutable historical ledger; `[Unreleased]` section records the removal. Practical impact low. |
| F-06 | `tests/ExceptionUsageLintTest.php:199-210` | `removeRecursively` does not suppress or check `rmdir()`/`unlink()` return values. | nit | **fixed** — all calls now use `@` suppression with `trigger_error(E_USER_WARNING)` fallback. |
| F-07 | `tests/ExceptionUsageLintTest.php:173` | `runScript` passes only `PATH` to the subprocess. | nit | **fixed** — now passes `[...getenv(), ...$env]`, inheriting full parent environment overlaid with explicit overrides. |
| F-08 | `bin/check-exception-usage.php:149-157` | `::class` keyword produces `T_CLASS` token; inner loop `continue` on non-array tokens skips past operators (`.`, `;`) and may capture an unrelated `T_STRING` as a declared type name. E.g. `Foo::class . SOME_CONST;` records `SOME_CONST`. Latent — no `::class` in `src/Exception/` today. | medium | open — fix: change `continue` to `break` for non-array tokens; add regression test with `::class` before a real declaration. |
| F-09 | `bin/check-exception-usage.php:141` | Only `T_INTERFACE` and `T_CLASS` are checked; enums (`T_ENUM`) and traits (`T_TRAIT`) in `src/Exception/` would not be discovered. Latent — no enums/traits in `src/Exception/` today. | low | open — add `T_ENUM` and `T_TRAIT` to the condition if desired; low priority. |
| F-10 | `UPGRADE.md:494` | `NoResponseStrategyException` (extends `\LogicException`) missing from `\LogicException` row in Before/After table. Pre-existing, not introduced by this PR. | low | open — pre-existing; could be fixed by adding to the `\LogicException` row, but out of scope for #593. |
| F-11 | `tests/ExceptionUsageLintTest.php:249,253,259` | `trigger_error(E_USER_WARNING)` in `removeRecursively` invokes the PHP error handler even under `@` (per DEC-017); could escalate to `ErrorException` under a strict handler. In `tearDown` so practical risk minimal. | nit | open — consider switching to `error_log()` per DEC-017; not blocking. |

## Round 3

| F-08 | `bin/check-exception-usage.php:146-160` | `::class` keyword causes false-positive type discovery. | medium | **fixed** — token lookback for `T_DOUBLE_COLON` (lines 146–156) + `break` on non-array tokens (line 159–160). Regression test `testClassConstantFetchIsNotMistakenForADeclaration` added at `tests/ExceptionUsageLintTest.php:132–151`. Verified with isolated `php -r`: `Foo::class . SOME_CONST;` before `class Bar {}` → `["Bar"]` only (was `["SOME_CONST", "Bar"]` before fix). |
| F-09 | `bin/check-exception-usage.php:140` | Only `T_INTERFACE` and `T_CLASS` checked; enums and traits not discovered. | low | **fixed** — `T_TRAIT` added to `in_array` check, `T_ENUM` guarded with `\defined('T_ENUM')` for PHP < 8.1 compat. PHPDoc updated. Verified with `php -r`: trait → `["MyTrait"]`, enum → `["MyEnum"]`. No dedicated regression test; acceptable for latent gap. |
| F-10 | `UPGRADE.md:494` | `NoResponseStrategyException` missing from `\LogicException` row in Before/After table. | low | **not a real finding** — `NoResponseStrategyException` was created in commit e433ad5 (a ResponseConverter refactor), not in the 0.12 hierarchy migration commit e40331e. The Before/After table documents the hierarchy migration; `NoResponseStrategyException` has no "before" in that context. The hierarchy tree (line 521) correctly lists it. Git history: both commits shipped in v0.12.0, but the table's scope is the hierarchy replacement, not every exception introduced in the release. |
| F-11 | `tests/ExceptionUsageLintTest.php:270,274,280` | `@trigger_error(E_USER_WARNING)` in `removeRecursively` invokes the error handler even under `@` (per DEC-017). | nit | **still present** — `@trigger_error(..., E_USER_WARNING)` remains in `removeRecursively`. Non-blocking: tearDown context, PHPUnit does not escalate `E_USER_WARNING` to `ErrorException`. Recommended fix: switch to `error_log()` per DEC-017. Not blocking merge. |

## CI failure (PR #814, 2026-09-10) — recorded as escaped finding per workflow step 11
- File: tests/WorkermanCommandTest.php — `testReloadDoesNotBreakServer`
- Severity: low (flaky E2E, infra-timing)
- What: single error on leg Tests (8.5, 8.0.*) only — `ConnectException: cURL error 52: Empty reply from server` for `http://127.0.0.1:8888/response_test` during reload test. Other 8 matrix legs passed; lint passed. No relation to this PR's diff (exception wiring + tokenizer lint script + docs) — the failing path exercises daemon reload timing, untouched by this change.
- Action: re-run of the failed job, no code change.
