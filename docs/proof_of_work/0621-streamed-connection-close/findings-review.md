# Findings (review) — issue #621

| ID | Severity | File:line | What is wrong | What happened |
|----|----------|-----------|---------------|---------------|
| F-1 | medium | src/Middleware/SymfonyController.php:129-131 | The new `$shouldClose` computation in `SymfonyController::__invoke()` is not tested. `tests/SymfonyControllerTest.php` has `testStreamedResponseE2E` but it uses an HTTP/1.1 request without `Connection: close`, so `$shouldClose` is `false` and the close path is never exercised at the controller level. The duplicated two-line check (`protocolVersion() === '1.0' \|\| strcasecmp(...'close') === 0`) is the sole signal a streamed head gets for `Connection: close`; if it drifts from `HttpRequestHandler::shouldCloseConnection()` the handler closes the socket while the head stays silent — reproducing this exact issue. Trigger: an HTTP/1.1 `Connection: close` request for a `StreamedResponse` route. | |
| F-2 | medium | src/Http/Response/ResponseConverter.php:36 | The new `bool $shouldClose` parameter on `ResponseConverter::convert()` is threaded to method-aware strategies (line 53) but no test in `tests/ResponseConverterTest.php` asserts it is passed through. All existing converter tests call `convert()` with 4 args (relying on the default). A regression that drops the `$shouldClose` argument from the method-aware dispatch would not be caught. Trigger: any change to the `instanceof` dispatch branch. | |
| F-3 | low | src/Middleware/SymfonyController.php:129-131 vs src/Http/HttpRequestHandler.php:207-210 | The close-intent check is duplicated verbatim in `SymfonyController::__invoke()` and `HttpRequestHandler::shouldCloseConnection()`. Both must be updated in lockstep if the rule changes (e.g. honouring `Connection: keep-alive` on HTTP/1.0, or a future protocol version). A mismatch silently reproduces issue #621. The coder already flagged this (findings-coder #1); a single shared helper (`Http\ConnectionIntent::shouldClose(Request)`) would eliminate the drift hazard. | |
| F-4 | nit | src/Http/Response/Strategy/BinaryFileResponseStrategy.php:37 | `bool $shouldClose = false` is declared but never read. Intentional (the strategy's responses go through `sendResponse()` central stamping) and documented in the comment at line 50, but the contract "ignores close intent" lives only in prose — PHPStan does not flag unused parameters. No behavior impact. | |
